spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ramli_db
    username: root
    password: saloum

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
